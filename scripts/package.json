{
  "name": "innertable-scripts",
  "version": "1.0.0",
  "description": "One-off utility scripts for InnerTable (migrations, etc.)",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "migrate:dry":     "node migrate-firebase-to-supabase.js",
    "migrate:execute": "node migrate-firebase-to-supabase.js --execute"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4",
    "dotenv": "^16.4.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
