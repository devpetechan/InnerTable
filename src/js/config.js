// ══════════════════════════════════════════════════
//  CONFIG
//
//  All secrets (SUPABASE_URL, SUPABASE_ANON_KEY, GOOGLE_MAPS_API_KEY)
//  are loaded from src/js/env.js, which is gitignored and never committed.
//  See src/js/env.example.js for setup instructions.
//
//  Admin status is stored in the database (public.users.is_admin) and
//  read at sign-in time — no passwords in client-side code.
// ══════════════════════════════════════════════════
