# ── Secrets & local environment files ────────────────────────────────────────
# These files contain API keys and must NEVER be committed.
.env
.env.local
.env.*.local
scripts/.env.migration
# NOTE: src/js/env.js is intentionally NOT gitignored so GitHub Pages can serve it.
# The Supabase anon key is safe to expose publicly — RLS protects the data.
# Restrict the Google Maps API key to your domain in Google Cloud Console.

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/
scripts/node_modules/

# ── OS files ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Editor files ─────────────────────────────────────────────────────────────
.vscode/settings.json

# ── Supabase ─────────────────────────────────────────────────────────────────
supabase/.branches
supabase/.temp
supabase/.env.keys
supabase/.env.local
supabase/.env.*.local
