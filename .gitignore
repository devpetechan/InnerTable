# ── Secrets & local environment files ────────────────────────────────────────
# These files contain API keys and must NEVER be committed.
.env
.env.local
.env.*.local
scripts/.env.migration
# NOTE: src/js/env.js is intentionally NOT gitignored so GitHub Pages can serve it.
# The Supabase anon key is safe to expose publicly — RLS protects the data.
# Restrict the Google Maps API key to your domain in Google Cloud Console.

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/
scripts/node_modules/

# ── OS files ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Editor files ─────────────────────────────────────────────────────────────
.vscode/settings.json

# ── Supabase ─────────────────────────────────────────────────────────────────
supabase/.branches
supabase/.temp
supabase/.env.keys
supabase/.env.local
supabase/.env.*.local
# NOTE: supabase/snippets/ was gitignored until v0.5.0 Phase 1 — acceptance
# tests are real deliverables and belong in the repo (decided 2026-07-22).

# ── Local-only data dumps (contain real user PII — never commit) ────────────
prod-data.sql

# ── Local tool state & OS noise ──────────────────────────────────────────────
.claude/
.DS_Store
