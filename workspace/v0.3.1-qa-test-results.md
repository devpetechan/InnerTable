# v0.3.1 (REL-19) — QA Test Results (2026-07-07)

Executed by Claude against the local build (`python3 -m http.server 8765`).
Automated/static checks below; **manual browser steps (IT-088) need you** in
Chrome + Safari at desktop and mobile viewport.

## Automated / static — passed

| Check | Result |
|---|---|
| JS syntax (`app.js`, `ui-events.js`, `ui-render.js`, `places-service.js`, `map.js`) | ✅ |
| Hanken Grotesk link in `index.html` | ✅ |
| Design tokens block in `main.css` (`:root`, IT-084) | ✅ |
| 4b card renderer (`placeCardHTML`, signal/top-take/footer) | ✅ |
| 2a list markup (view-tabs, display-mode-toggle, filter-sort-btn, FAB) | ✅ |
| 6b flow markup (step-place/intent/review, flow-dots, sticky submit) | ✅ |
| Write path still routes through `submitEntry()` (IT-036) | ✅ |
| `addTakeForPlace()` skips step 1 (`skipPlaceStep` + `goToStep(2)`) | ✅ |
| Google rating fallback when `aggregate.avgRating === 0` | ✅ |
| Map pin colors use design tokens (clay / try-blue) | ✅ |
| Map loading/error copy — no emoji (design/CLAUDE.md) | ✅ |
| Intent cards — keyboard Enter/Space handlers | ✅ |
| `clearForm()` resets `flowStep` + step visibility | ✅ |

## Code review against acceptance criteria

| Ticket | Verdict | Notes |
|---|---|---|
| IT-084 | ✅ Implemented | Tokens cover palette, chips, avatars, stars, spacing, shadows; `.micro-label` utility present |
| IT-085 | ✅ Implemented | 4b row structure, chip states, avatar stack, Google degrade, footer CTA variants |
| IT-086 | ✅ Implemented | Segmented status + List/Map wired to existing filter logic; Filter & sort stub reveals chips |
| IT-087 | ✅ Implemented | 3-step flow; want-to-try exits at step 2; disclosure for optional fields; places-service unchanged |
| IT-088 | ⏳ Manual pending | Plan in `workspace/v0.3.1-qa-test-plan.md` — run in your browsers |

## Not tested in browser — needs you

- Visual pixel-compare against `design/*.dc.html` mockups
- Google Places autocomplete dropdown (needs live Maps API key + sign-in)
- Full add/edit/delete flows against Supabase (needs Google sign-in on localhost)
- Map pin dedup with real coords (needs Maps API + signed-in session)
- Safari-specific rendering (font weights, sticky modal footer, safe-area)
- Mobile viewport FAB + bottom-sheet modal ergonomics
- Realtime cross-window updates (known publication gap from IT-035 QA)
- Comment threads + reactions under the new card chrome

## Polish applied this session

- Replaced remaining ad hoc hex colors in redesigned surfaces with CSS custom properties
- Place-card hairline now uses `--color-divider` (was canvas color)
- Map empty/error states no longer use emoji
- `clearForm()` resets flow step visibility when modal closes

---

**Next:** Run `workspace/v0.3.1-qa-test-plan.md` manually, then mark IT-088 done and
ship REL-19 when satisfied.
