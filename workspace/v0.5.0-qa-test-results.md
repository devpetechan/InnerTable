# v0.5.0 QA Test Results — Explicit Taste Preferences (REL-12), Phase 3 (IT-047)

**Date:** 2026-07-24 · **Plan:** `workspace/v0.5.0-qa-test-plan.md` · **Verdict: PASS (Parts 0–7), pending two manual items**
**Environment:** deployed prod app (`https://devpetechan.github.io/InnerTable/`), signed in as `dev.pete.chan@gmail.com`.
Scope run: the casual 5-dot taste grid (IT-047, Phase 3) — the only frontend surface shipped. Parts 8–10 (Phases 4–6) not built yet, not tested.
Automated by Claude via the Chrome plugin (real dot clicks + RLS-respecting `supabaseClient` DB checks). Baseline: `friend_taste_overrides` was empty before the run.

## Part 0 — Environment sanity

| # | Result |
|---|---|
| 0.1 | ✅ App loads on List screen; `supabaseClient` present; no taste-service errors observed |
| 0.2 | ✅ Four tabs: My Friends · Requests · Find · **Taste** |
| 0.3 | ✅ Taste tab renders the grid (account has 7 accepted friends) |

## Part 2 — Taste grid rendering (IT-047)

| # | Result |
|---|---|
| 2.1 | ✅ One row per accepted friend (7), alphabetical case-insensitive (BobKat, crazypete04, PC, pete.chan, Peter, Peter Chan, robertkatayama); name column pinned |
| 2.2 | ✅ 6 default category columns (American, Italian, Mexican, Japanese, Chinese, Thai); each cell = 5 empty `tg-dot` spans (role=button) |
| 2.3 | ✅ Name column `position: sticky; left: 0`; stayed pinned at left=240 while the body scrolled 800px under the 19-column expanded overflow (scrollWidth 2187 vs container 818) |
| 2.4 | ✅ Helper line matches exactly: "More dots = you trust their pick more in that category. Tap a filled dot again to clear it." |
| 2.5 | n/a — account has accepted friends |

## Part 3 — Save-as-you-go + persistence

| # | Result |
|---|---|
| 3.1 | ✅ Tapped dot 3 on BobKat×American → dots 1–3 filled immediately (optimistic) |
| 3.2 | ✅ After ~1s, one row (BobKat, American) with `weight = 3` |
| 3.3 | ✅ Tapped dot 5 → still exactly one row, `weight = 5` (upsert, no duplicate) |
| 3.4 | ✅ Tapped dot 1 → `weight = 1` |
| 3.5 | ✅ Switched to My Friends and back → cell still shows 1 filled |
| 3.6 | ✅ Full page reload → cell still shows 1 filled (persistence confirmed) |

## Part 4 — "Show all categories" expander

| # | Result |
|---|---|
| 4.1 | ✅ Expanded to full 19-category seed set; button label flipped to "Show fewer categories" |
| 4.2 | ✅ BobKat×American weight retained after expanding |
| 4.3 | ✅ Set Peter×Indian (expanded-only column) to 4 → new row persisted (`weight = 4`) |
| 4.4 | ✅ "Show fewer categories" collapsed back to the 6-column short list; label reverted |

## Part 5 — NULL vs 0 invariant (critical) + clear = delete

| # | Result |
|---|---|
| 5.1 | ✅ Tapped the current level (dot 1) on BobKat×American → cell cleared to 0 filled dots |
| 5.2 | ✅ After ~1s, **no row** for (BobKat, American) — row was DELETEd, not set to `weight = 0` |
| 5.3 | ✅ Full scan: no casual-grid row has `weight = 0` anywhere (`anyWeightZero = false`) |
| 5.4 | ✅ Reload → cleared cell still empty; other value (Peter×Indian=4) intact in DB |
| 5.5 | ✅ Weight distribution after the run = `{4: 1}` only — no `0`, no `NULL` from the casual grid |

**Invariant confirmed: `NULL` ≠ `0`, and the casual grid never writes `0`.**

## Part 6 — Most-used personalization

| # | Result |
|---|---|
| 6.1 | ✅ Default columns are non-empty and padded with seed-order categories (American→Thai = sort_order 10–60) |
| 6.2 | ⚠️ **Inconclusive on this account.** The signed-in account (`dev.pete.chan`) has **no own rated places** — every take in the list belongs to other accounts — so "cuisines you've rated most appear first" has nothing to rank and correctly falls back to seed order. The positive "used-a-lot cuisine surfaces first" behavior could not be demonstrated here. Recommend re-running Part 6 on an account that has several own ratings. |

## Part 7 — v0.4.x regression (spot checks)

| # | Result |
|---|---|
| 7.1 | ✅ My Friends (7 friends), Requests (Incoming/Outgoing empty states), Find all render; search "peter" returns Peter + Peter Chan with correct "Friends ✓" badges |
| 7.2 | ✅ Friend profile opens (Peter: "9 places", their-places list, Remove/Block); "‹ Friends" back returns to My Friends list with Taste tab intact |
| 7.3 | ✅ Add-flow modal renders (step 1/2: place search, location, Restaurant/Bar type, gated Next); status tabs filter (Want to Try → 1 place); Filter & sort opens (BY / TYPE / SORT); Map toggles and shows location markers |
| 7.4 | ⏸ **Not run — needs Peter.** Remove friend uses a native `confirm()` dialog that freezes automation, and the action is destructive. |
| 7.5 | ⏸ **Not automated.** The Chrome plugin couldn't force the viewport below ~1298px on this display, so the true mobile render wasn't captured. The mobile-critical mechanism (horizontal-scroll grid + sticky name column) is confirmed in 2.3/4; recommend eyeballing the 4-tab fit on a phone. |

## Observations / follow-ups

- **Map base tiles:** in Map view the location markers render but the base map tiles show as a grey placeholder. This is pre-existing map behavior unrelated to the Phase 3 taste changes; noting for awareness, not logging as an IT item against REL-12.
- **Leftover test data:** one test row remains in `friend_taste_overrides` — Peter×Indian `weight = 4` (created for step 4.3). Delete before sign-off if you want a clean slate.

## Sign-off checklist

- [x] Parts 0–5 pass on deployed prod
- [x] `NULL` vs `0` invariant confirmed (Part 5) — no `weight = 0` from casual grid
- [x] Part 7.1–7.3 regression pass
- [ ] Part 6.2 re-run on an account with own ratings (personalization positive case)
- [ ] Part 7.4 Remove-friend (Peter clicks the native confirm) → removed row gone from grid
- [ ] Part 7.5 mobile layout eyeballed on a phone
- [ ] Later parts (8–10) added and passed as Phases 4–6 ship

---

# Phase 4 QA — Global "Advanced details" toggle (IT-049) + quant Taste grid (IT-048)

**Date:** 2026-07-25 · **Verdict: PASS (Parts A, B, C)**
**Environment:** deployed prod app (`https://devpetechan.github.io/InnerTable/`, GitHub Pages), signed in as `dev.pete.chan@gmail.com` (id `d73b2b96…`).
Automated by Claude via the Chrome plugin — real UI toggling + `<select>` `change` events through the app's own handler, verified against the RLS-respecting `supabaseClient`. **Baseline:** `friend_taste_overrides` empty and `show_advanced_details = false` at start (the Phase 3 leftover Peter×Indian row noted previously was already gone).

**Migration 0023 (`users.show_advanced_details`):** applied in the Supabase SQL editor.
- Schema verify: `boolean | NO | false`. Row scan: 0 NULLs, 8/8 rows defaulted to `false`.
- `test_0023_show_advanced_details.sql`: **7/7 PASS**, 0 failures (boolean · NOT NULL · DEFAULT false · new-signup default · A own-row UPDATE · B UPDATE 0 rows · A untouched).

## Part A — Toggle flips both surfaces + persists (IT-049)

| # | Result |
|---|---|
| A1 | ✅ Account menu → "Advanced details" ON → Taste grid immediately quant: `<select>` cells, full 19-category column list, quant helper ("Precise trust per category. 0 = mute…; — = no opinion / use default"), no "Show all categories" |
| A2 | ✅ Hard-refresh → still quant; console `show_advanced_details === true` |
| A3 | ✅ Toggle OFF → back to 5-dot casual with "Show all categories"; hard-refresh → still casual; console `show_advanced_details === false` |

## Part B — Casual ↔ quant show the same weights (two skins, one dataset)

| # | Result |
|---|---|
| B1 | ✅ Casual: BobKat×American set to 3 dots → after ~1s, one row (BobKat, American) `weight = 3` |
| B2 | ✅ Toggle to quant → same cell's `<select>` reads `3` |
| B3 | ✅ Quant: change to 5 → toggle to casual → cell shows 5 filled dots |
| B4 | ✅ Console: that row `weight = 5` (same friend_id/category_id, upsert — no duplicate) |

## Part C — Quant 0 (mute) vs NULL/default distinct (IT-048 core)

| # | Result |
|---|---|
| C1 | ✅ Quant: BobKat×American (F1×C1) set to "0" → after ~1s, a row for (F1,C1) with `weight = 0` |
| C2 | ✅ BobKat×Italian (F1×C2): set to 4 (row created), then cleared to "—" → row **DELETEd**, not left as `weight = 0` (no row for F1,C2) |
| C3 | ✅ Hard-refresh → quant: (F1,C1) still "0" with dashed/dimmed muted styling; (F1,C2) still "—". DB: single row (American `weight = 0`), no Italian row. 0 round-trips DISTINCT from absent/NULL |

**Invariant confirmed: explicit `0` (mute) persists as a row; "—" is absence (DELETE / NULL). The two are distinct across reload.**

## Phase 4 leftover test data

- One row remains in `friend_taste_overrides`: **BobKat × American, `weight = 0`** (the explicit-mute cell from Part C3). Left in place per QA convention; delete from the app console (RLS-scoped) if you want a clean slate.
